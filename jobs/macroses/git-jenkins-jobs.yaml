- scm:
    name: git-ui-tests
    scm:
    - git:
        url: git@github.com:Vanchette/PractickOtus.git
        credentials-id: jenkins
        branches:
        - FETCH_HEAD
        refspec: ${BRANCH}
- scm:
    name: git-api-tests
    scm:
    - git:
        url: git@github.com:Vanchette/restAssured.git
        credentials-id: jenkins
        branches:
        - FETCH_HEAD
        refspec: ${BRANCH}